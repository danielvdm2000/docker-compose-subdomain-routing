# Using nginx with docker compose for subdomain routing
### Start command
`docker compose up`

### http://admin.mysite.localhost
Admin service in docker compose

### http://portal.mysite.localhost
Portal service in docker compose
